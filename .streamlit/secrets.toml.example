# Rename this file to secrets.toml and add your key to pre-configure AI for all users.
# On Streamlit Community Cloud, paste this content into the Secrets section of your app settings.
# NEVER commit secrets.toml to Git — it's in .gitignore.

OPENAI_API_KEY = "sk-..."
